{% extends 'base.html.twig' %}
{% block body %}
	<h2 class="page-header text-warning">Edit Event</h2>
	<div class="wrapper">
		{{form_start(form)}}
		{{form_widget(form)}}
		{{form_end(form)}}
	</div>
{% endblock %}
